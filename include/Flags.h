#ifndef FLAGS_H
#define FLAGS_H

extern bool signalRunning; // Para verificar si la senal se esta ejecutando
extern bool firstOpen; // Verifica si es la primera vez que se abre el directorio
extern bool noSDFound; // Verifica si la targeta sd no fue encontrada
extern bool filling; // Verifica si se esta relllenando el buffer de escritura

#endif